[package]
name = "framework"
version = "0.4.2"
edition = "2024"

[dependencies]
tracing.workspace = true
tracing-subscriber = "*"

serde.workspace = true
serde_json = "*"

tokio.workspace = true
tokio-util = { version = "*", features = ["rt"] }

http = "*"
axum.workspace = true
axum-extra = { version = "*", features = ["cookie"] }
axum-server = { version = "*", features = ["tls-rustls"] }
tower-http = {version ="*", features = ["fs"]}

reqwest = { version = "0", features = ["stream"] }

rdkafka = { version = "*", default-features = false, features = [
    "dynamic-linking",
    "tokio",
    "zstd",
], optional = true }

sqlx = { version = "*", features = ["runtime-tokio", "tls-rustls-aws-lc-rs", "postgres", "mysql"], optional = true }

chrono.workspace = true
uuid = { version = "*", features = ["v7"] }

futures = "*"
bytes = "*"
base64 = "*"
bs58 = "*"
indexmap = { version = "*", features = ["serde"] }

[features]
db = ["dep:sqlx"]
kafka = ["dep:rdkafka"]
