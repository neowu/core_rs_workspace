[package]
name = "framework"
version = "0.4.2"
edition = "2024"

[dependencies]
tracing.workspace = true
tracing-subscriber = { version = "*", default-features = false, features = ["std", "registry", "fmt"] }

serde.workspace = true
serde_json = "*"

tokio.workspace = true
tokio-util = { version = "*", features = ["rt"] }

http = "*"
axum.workspace = true
axum-extra = { version = "*", features = ["cookie"] }
axum-server = { version = "*", features = ["tls-rustls"] }
tower-http = {version ="*", features = ["fs"]}

reqwest = { version = "0", features = ["stream"] }

rdkafka = { version = "*", default-features = false, features = [
    "dynamic-linking",
    "tokio",
    "zstd",
], optional = true }

tokio-postgres = { version = "*", features = ["default", "with-chrono-0_4"], optional = true }

chrono.workspace = true
uuid = { version = "*", features = ["v7"] }

futures = "*"
bytes = "*"
base64 = "*"
bs58 = "*"
indexmap = { version = "*", features = ["serde"] }

[features]
db = ["dep:tokio-postgres"]
kafka = ["dep:rdkafka"]
