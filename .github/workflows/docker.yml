name: Docker
on:
  push:
    tags: ["*.*.*"]
env:
  CARGO_TERM_COLOR: always
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v2
        with:
          username: neowu
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - run: echo "TAG=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: app/log_exporter/Dockerfile
          tags: "neowu/log-exporter:${{ env.TAG }}-rust"
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: app/log_collector/Dockerfile
          tags: "neowu/log-collector:${{ env.TAG }}-rust"
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: app/log_processor/Dockerfile
          tags: "neowu/log-processor:${{ env.TAG }}-rust"
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
